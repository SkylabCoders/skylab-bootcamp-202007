<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Organic Market</title>
    <link rel="stylesheet" href="../css/header.css" />
    <link rel="stylesheet" href="../css/index.css" />
    <link rel="stylesheet" href="../css/signin.css" />
    <link rel="stylesheet" href="../css/footer.css">
</head>

<body>
    <%- include('../header'); %>
    <div class="container">
        <div class="signin_container">
            <h2 class="title__sign" ><%= user.user.toUpperCase()%> Profile!</h2>
            <form action="/auth/profile" method="POST">
                <div>
                    <label class="input" for="password">
                        New password:
                        <input class="input__invalid" type="password" name="password" required minlength="8" maxlength="12"
                            pattern="[^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#!/=*]).{8, 12}$" />
                    </label>
                </div>
                <div class="container__button">
                    <button class="button" type="submit">Save</button>
                </div>
            </form>
            <form class="container__button" action="/auth/logout" method="POST">
                <button class="button" type="submit" name="logout">Logout</button>
            </form>
        </div>
    </div>  
    <%- include('footer')%>
</body>

</html>