<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mongolitos</title>
    <link rel="stylesheet" href="../../css/index.css">
    <link rel="stylesheet" href="../../css/foodList.css">
    <link rel="stylesheet" href="../../css/header.css">
    <link rel="stylesheet" href="../../css/footer.css">
</head>

<body>
    <%- include('header')%>

    <ul class="container-products">
        <% for(let i = 0; i< cartItems.length; i++) { %>
        <li class="products-card">
            <a href="/products/<%=cartItems[i]._id%>">
                <img class="image__product box-image" src="<%= cartItems[i].img %>">
                <p class="text__type"><%=cartItems[i]._id%></p>
            </a>
            <div>
                <p class="text__price"><%= cartItems[i].amount%> € </p>
                <form method="POST" action="/products/<%=cartItems[i]._id%>">
                    <button class="button__addCart">Add to cart</button>
                </form>
            </div>
        </li>
        <% } %>
    </ul>

    <%- include('footer')%>
</body>

</html>