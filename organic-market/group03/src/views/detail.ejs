<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>The Best Organic Market!!</title>
		<link rel="stylesheet" href="/css/styles.css" />
	</head>
	<body>
		<%- include('header') %>
		<h2><%=title%></h2>
		<div>
			<% if (admin === 'on'){%>
			<div>
				<form action="" method="POST" name="recipeForm">
 					<label for="product__Title">Title:</label>
					<input
						id="product__Title"
						name="updatedTitle"
						placeholder="New title"
						value="<%= recipe.title %>"
						required
					/>
					</br>
					</br>

					<label for="product__Description">Description:</label>
					</br>
					<textarea class="text-area"
					minlength="4" maxlength="350"
						id="product__Description"
						name="updatedDescription"
						placeholder="New description"
						rows="10" cols="50"
						value="<%= recipe.description %>"
					><%= recipe.description %></textarea>

					</br>
					</br>

					<label for="product__Price">Price:</label>
					<input
						id="product__Price"
						name="updatedPrice"
						placeholder="New price"
						value="<%= recipe.price %>"
						required
					/>$
				</br>
				</br>

					<button
					type="submit"
					>
						Save changes
					</button>
				</form>

			</div>
			<% } else{ %>
			<h3><span>Title: </span><%= recipe.title %></h3>
			<div><span>Description: </span><%= recipe.description %></div>
		</br>
			<div><span>Price: </span><%= recipe.price %>$</div>
			<%}%>
		</div>
	</body>
</html>
