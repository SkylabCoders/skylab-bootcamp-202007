<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Tour of Heroes</title>
	<link rel="stylesheet" href="../css/index.css" />
	<link rel="stylesheet" href="../css/heroes.css" />
</head>

<body>
	<%- include('header'); -%>
	<h2><%= title %></h2>

	<form action="/heroes/addHero" method="POST">
		<label>Hero name:
			<input type="text" name="addHero" placeholder="Add a new hero..."
				value="" />
		</label>
		<button title="add hero" type="submit" name="addHero">
			add
		</button>
	</form>

	<ul class="heroes">
		<% for(let i= 0; i< heroes.length; i++) { %>
		<li>
			<form action="" method="POST">
				<a href="/heroes/<%= heroes[i]._id %>">
					<span class="badge"><%= heroes[i].id %></span> <%= heroes[i].name %>
				</a>
				<button class="delete" title="delete hero" type="submit"
					value="<%= heroes[i]._id %>" name="deleteHero">
					x
				</button>
			</form>
		</li>
		<% } %>
	</ul>
</body>

</html>