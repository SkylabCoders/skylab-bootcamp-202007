describe('hero-list', function () {
  beforeAll(function () {
    let hero = new HeroListComponent();
  });

  it('should create a hero list component', function () {
    expect(heroListComponent).toBeTruthy();
  });
});
