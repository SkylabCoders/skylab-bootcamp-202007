import React from 'react';

function Dashboard(props) {
	console.log(props.heroList);
	const heroesPromoted = function (heroList) {};
	return (
		<div>
			<h1>Dashboard</h1>;<h2></h2>
		</div>
	);
}

export default Dashboard;
