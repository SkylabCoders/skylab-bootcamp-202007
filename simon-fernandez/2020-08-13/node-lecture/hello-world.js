const http = require('http');

const myServer = (request, resonse) => response.end('Bombasto presidente');
const server = http.createServer(myserver);

server.listen(666, () => console.log('celeritas es un mierdas'));
