<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Hero List</title>
		<link rel="stylesheet" href="css/index.css" />
		<link rel="stylesheet" href="css/heroes.css" />
	</head>
	<body>
		<%- include('header')%>
		<h2><%= title%></h2>

		<form action="/heroes" method="POST">
			<label
				>Hero name:
				<input #heroName name="newHero"/>
			</label>
			<button  type="submit" name="newHero">
				add
			</button>
			<button type="submit" name="deleteAll">
				Delete All
			</button>
		</form>
			<ul class="heroes">
				<% for(let i = 0; i<heroes.length; i++){%>
					<li>
						<a href="/heroes/<%=heroes[i]._id%>">
							<span class="badge"><%= heroes[i].id %></span> <%= heroes[i].name %>
						</a>
						<form action="/heroes" method="POST">
							<button type="submit"  value="<%= heroes[i]._id %>" name="heroId" class="delete" title="delete hero">
								x
							</button>
						</form>	
					</li>
		<% } %>
		</ul>
	</body>
</html>
