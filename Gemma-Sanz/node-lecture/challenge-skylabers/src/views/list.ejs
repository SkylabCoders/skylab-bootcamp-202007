<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Skylab Directory</title>
		<link rel="stylesheet" href="css/styles.css" />
		<link rel="stylesheet" href="css/list.css" />
	</head>
	<body>
		<%- include('header') -%>
		<h2>Skylab List</h2>
		<div>
			<label
				>Skylaber name:
				<input #heroName />
			</label>
			<button>
				add
			</button>
		</div>

		<ul class="heroes">
            <% for(let i = 0; i<skylabers.length; i++){ %>
                <li>
                    <a href="/skylabers/<%=skylabers[i].id%>">
                        <span class="badge"><%=skylabers[i].id%></span> <%=skylabers[i].name%> <%=skylabers[i].lastName%>
                    </a>
                    <button class="delete" title="delete hero">
                        x
                    </button>
                </li>
            <% } %>
		</ul>
	</body>
</html>
