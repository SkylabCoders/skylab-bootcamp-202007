<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: sans-serif;
}

.lightbox {
    max-width: 20rem;
    height: auto;
    padding: 1rem 2.5rem;
    margin: 10% auto;
    box-shadow: 0 2px 2px rgba(10, 16, 20, .24), 0 0 2px rgba(10, 16, 20, .12);
    border-radius: 4px;
    background-color: #fff;
}

.title {
    color: #969696;
    margin-bottom: 1rem;
}

.button {
    background-color: #eee;
    color: #557584;
    margin-right: .5rem;
    padding: .5rem 1rem;
    border-radius: .3rem;
    cursor: pointer;
    text-decoration: none;
}

.title-hero {
    color: #404040;
    margin: 1.5rem 0;
}

.hero__list {
    
}

.hero__list a {
    display: flex;
    margin-bottom: .6rem;
    padding-left: .8rem;
    text-decoration: none;
    
}

.hero__list a span:nth-child(1),
.hero__list a span:nth-child(2) {
    margin-bottom: .6rem;
}

.hero__list a span:nth-child(1) {
    background-color: #557584;
    color: #fff;
    text-align: center;
    padding: .5rem;
    border-radius: .3rem 0  0 .3rem;
}

.hero__list a span:nth-child(2) {
    background-color: #eee;
    color: #969696;
    padding: .6rem 0 0 .8rem;   
    border-radius: 0 .3rem .3rem 0;
    width: 8rem;
}
</style>