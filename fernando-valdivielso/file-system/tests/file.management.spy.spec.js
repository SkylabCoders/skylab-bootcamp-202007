const { expect } = require('chai')

describe('File Management', () => {
    it('should creat a new file', () => {
        expect(true).to.be.true;
    });

    it('should not create a file when there is no filename', () => {
        expect(true).to.be.true;
    });
});