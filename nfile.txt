
const util = require('util');
const { writeFile, readFile } = require('fs').promises;


async function main() {
    const data = await readFile(__filename);

    const nfile = await writeFile('nfile.txt', data)
    console.log('File data is ' + data);

}
main();

console.log('test');