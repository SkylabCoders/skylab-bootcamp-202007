import header from './Header';
describe('Header', () => {
	it('should create', () => {
		expect(header).toBeDefined();
	});
	/* it('should update lowfat.state', () => {
         let lowfat = false;
         lowfat = header.changePropertyBoolean(lowfat, header.setLowFat);
         expect(lowfat).toEqual(true);
     })*/
});
