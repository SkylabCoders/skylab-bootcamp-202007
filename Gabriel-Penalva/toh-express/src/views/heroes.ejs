<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>heroes</title>
    <link rel="stylesheet" href="css/heroes.css">

</head>

<body>
    <%- include('header'); -%>
    <h2><%= title %></h2>

    <div>
        <label>Hero name:
            <input />
        </label>
        <!-- (click) passes input value to add() and then clears the input -->
        <button>
            add
        </button>
    </div>

    <ul class="heroes">
        <% for(let i = 0; i < heroes.length; i++) {%>
        <li>
            <a href=" /heroes/<%= heroes[i]._id %>">
                <span class="badge"><%=heroes[i].id%></span> <%=heroes[i].name%>
            </a>
            <form action="/heroes" method="post" name='delhero'>
                <input name="id" type="hidden" value="<%=heroes[i]._id%>">
                <button class="delete" type="submit" title="delete hero">x</button>
            </form>

        </li>
        <% } %>
    </ul>
</body>

</html>